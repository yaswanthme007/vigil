# Vigil — environment variables to set in the Railway project (Variables tab).
# Copy these keys and paste your real values in Railway; do NOT commit real keys.

QDRANT_URL=
QDRANT_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
ENKRYPT_API_KEY=
